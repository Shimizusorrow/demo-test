spring:
  test:
    database:
      replace: none
  datasource:
    url: jdbc:h2:mem:testdb;DATABASE_TO_LOWER=TRUE;MODE=MySQL; #MODE=MYSQL 引入mysql语法特性
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update