spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/blog?useSSL=false&characterEncoding=utf-8&serverTimezone=GMT%2b8
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
server:
  port: 8090
  servlet:
    context-path: /admin

